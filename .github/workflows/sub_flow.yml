name: Build

on:
  workflow_call:
    inputs:    
      fake:
        type: string
        required: true
      
jobs:
  build: 
    runs-on: ubuntu-latest
    steps: 
      - uses: actions/checkout@v4

      - name: Fake build true
        if: ${{ fake == "true" }}
        run: echo "Fake build"
      
      - name: Fake build false
        if: ${{ fake == "false" }}
        run: echo "Real build"
      