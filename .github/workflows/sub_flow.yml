name: Build

on:
  workflow_call:
    inputs:    
      fake:
        type: boolean
        required: true
      
jobs:
  build: 
    runs-on: ubuntu-latest
    steps: 
      - uses: actions/checkout@v4

      - name: Fake build true
        if: fake-build
        run: echo "Fake build"
      
      - name: Fake build false
        if: fake-build
        run: echo "Real build"
      